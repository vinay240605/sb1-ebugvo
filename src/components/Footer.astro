---
const currentYear = new Date().getFullYear();

const categories = [
  { name: 'Wellness', href: '/wellness' },
  { name: 'Travel', href: '/travel' },
  { name: 'Food', href: '/food' },
  { name: 'Lifestyle', href: '/lifestyle' }
];

const social = [
  { name: 'Instagram', href: '#', icon: 'instagram' },
  { name: 'Twitter', href: '#', icon: 'twitter' },
  { name: 'Pinterest', href: '#', icon: 'pinterest' }
];
---
<footer class="bg-gray-900 text-white mt-20">
  <div class="max-w-6xl mx-auto px-4 py-12">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <div class="col-span-1 md:col-span-2">
        <h3 class="text-2xl font-serif font-bold text-rose-500 mb-4">Lifestyle Journal</h3>
        <p class="text-gray-400">Exploring life's beautiful moments and sharing inspiring stories about wellness, travel, and personal growth. Join us on this journey of discovery.</p>
      </div>
      <div>
        <h3 class="font-semibold text-white mb-4">Categories</h3>
        <ul class="space-y-2">
          {categories.map(category => (
            <li>
              <a href={category.href} class="text-gray-400 hover:text-rose-500 transition-colors">
                {category.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div>
        <h3 class="font-semibold text-white mb-4">Connect</h3>
        <ul class="space-y-2">
          {social.map(item => (
            <li>
              <a href={item.href} class="text-gray-400 hover:text-rose-500 transition-colors">
                {item.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
    <div class="border-t border-gray-800 mt-12 pt-8 text-center text-gray-400">
      © {currentYear} Lifestyle Journal. All rights reserved.
    </div>
  </div>
</footer>